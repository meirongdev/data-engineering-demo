# Shared configuration — sourced by scripts/lib.sh and included by the Makefile.
# Override any value via environment variable before running make or scripts.
CLUSTER_NAME=data-eng
NAMESPACE=lakehouse
IMAGE=spark-iceberg:local
LOADGEN_IMAGE=loadgen:local
ICEBERG_REST_IMAGE=iceberg-rest:local
